import { FooterPortalDirective } from './footer-portal.directive';

describe('FooterPortalDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterPortalDirective();
    expect(directive).toBeTruthy();
  });
});
